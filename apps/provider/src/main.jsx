import React from 'react';
import ReactDOM from 'react-dom/client';
import AppAuthWrapper from './pages/ProviderDashboard';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppAuthWrapper />
  </React.StrictMode>
);
